import { make, notice, page, definitionList } from '../../shared/view-helpers.mjs';
import { managementRequest } from '../../shared/management-actions.mjs';
export async function renderRoute(context) { const id = encodeURIComponent(context.route.params.providerId); const data = await managementRequest(`/api/management/providers/${id}`); if (!context.isCurrent()) return; const view = page(`Provider ${data.provider.name}`, 'Safe diagnostics show a category and correlation, never submitted credential material.'); const feedback = make('div'); feedback.setAttribute('role', 'status'); const test = make('button', 'Test connection'); test.type = 'button'; const run = async (retry = false) => { test.disabled = true; try { const result = await managementRequest(`/api/management/providers/${id}/${retry ? 'retry' : 'test'}`, { method: 'POST', body: {} }); feedback.textContent = `${result.result.status || result.result.diagnostic || 'completed'} · ${result.correlationId}`; } catch (error) { feedback.replaceChildren(notice(error.message, { error: true })); } finally { test.disabled = false; } }; test.addEventListener('click', () => void run()); const retry = make('button', 'Retry test'); retry.type = 'button'; retry.addEventListener('click', () => void run(true)); view.node.append(definitionList(Object.entries(data.provider).filter(([key]) => !/credential|secret/i.test(key))), test, retry, feedback); context.root.replaceChildren(view.node); }
