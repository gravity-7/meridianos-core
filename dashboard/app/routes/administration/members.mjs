import { make, notice, page, table } from '../../shared/view-helpers.mjs'; import { managementRequest } from '../../shared/management-actions.mjs';
export async function renderRoute(context) { const [members, invitations] = await Promise.all([managementRequest('/api/management/access/memberships'), managementRequest('/api/management/access/invitations')]); if (!context.isCurrent()) return; const view = page('Members and invitations', 'Invitation lifecycle and membership changes are enforced by the server within the selected scope.'); const feedback = make('div'); feedback.setAttribute('role', 'status'); const form = make('form'); const email = make('input'); email.type = 'email'; email.required = true; const role = make('select'); for (const value of ['viewer', 'operator', 'admin']) { const option = make('option', value); option.value = value; role.append(option); } const invite = make('button', 'Invite member'); form.append(make('label', 'Email'), email, make('label', 'Role'), role, invite); form.addEventListener('submit', async (event) => { event.preventDefault(); try { const result = await managementRequest('/api/management/access/invitations', { method: 'POST', body: { email: email.value, role: role.value } }); feedback.textContent = `Invitation created · ${result.correlationId}`; void context.refresh(); } catch (error) { feedback.replaceChildren(notice(error.message, { error: true })); } }); const memberRows = members.memberships.map((member) => [member.email, member.role, member.version]); const invitationRows = invitations.invitations.map((item) => [item.email, item.role, item.status, item.expiresAt]); view.node.append(form, feedback, memberRows.length ? table(['Member', 'Role', 'Version'], memberRows, 'Scoped memberships') : notice('No members are listed in this scope.'), invitationRows.length ? table(['Invitation', 'Role', 'State', 'Expires'], invitationRows, 'Scoped invitations') : notice('No invitations are pending.')); context.root.replaceChildren(view.node); }
