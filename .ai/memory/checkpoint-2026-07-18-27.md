# Checkpoint — session #27 (resumed)

**Resumed digest.** Boot sequence completed per RESUME-PROMPT.md (cheap boot: continuity.json +
newest checkpoint + OWNERSHIP.md + `tail -c 4000` decision-log only).

## What was found in-flight
`continuity.json`/`OWNERSHIP.md` both claimed C6 (Docker/compose L2 packaging) was "dispatched" by
session #26 and "awaiting PR" in an isolated worktree. **That claim did not hold up:** `git branch -a`,
`git ls-remote --heads origin`, `git worktree list`, and `gh pr list` / `gh pr list --search "c6"` all
came back with no trace of a C6 branch or PR anywhere. Session #26 ended before its dispatched subagent
produced anything durable, and a claimed dispatch is not itself evidence of surviving work — trusting
git as ground truth (per RESUME-PROMPT's own rule) caught this. Redispatched C6 fresh this session,
same scope, Track-A, `isolation:'worktree'`, background.

## The lease scare (resolved as benign)
`.ai/state/orchestrator.lease` showed `acquired_at` only ~1 minute before this session's boot, which
matches the shape of the concurrency wrinkle flagged in checkpoint-22/-26 (D-035/D-036). This time,
process-enumeration commands (`tasklist`, `ps -W`) were blocked by the sandbox and could not be used to
check PID liveness directly. Instead, read `conductor.mjs`'s `decide()`/`inspectLease()`/spawn logic:
the conductor itself acquires the lease with the just-spawned child's own PID *before* handing off to
the LLM session — so a lease that's ~1 minute old at boot is the *expected*, benign shape of "the
conductor just spawned me," not evidence of a second concurrent orchestrator. No action needed; did not
touch the lease file. **Note for future sessions:** if `tasklist`/`ps` get blocked again, don't keep
retrying — reason from `conductor.mjs`'s source instead.

## Verification this session
- `npm test` from the primary checkout, no `AIOS_ROOT`: 862 tests, 853 pass, 8 skip, **1 fail**
  (`tests/harness-adapters.test.mjs`, `TypeError: Cannot read properties of null (reading 'cmd')`).
  Re-ran `node --test tests/harness-adapters.test.mjs` in isolation: **29/29 pass.** Matches the
  already-known, already-logged resource-contention flake — not a regression. `worktree.test.mjs`
  (the other known flake) did not fail this run.
- Main confirmed green at `410c17e`, unchanged since the last checkpoint — no drift, nothing to
  reconcile beyond the C6 non-dispatch above.

## Status
- Chokepoints free. Only in-flight work: C6 (redispatched this session).
- C7 (mos-dev thin-tenant rebuild) remains **not dispatchable from this session**: `/c/projects/mos-dev`
  is outside allowed working directories, and Gate-2 (real-spend) founder sign-off is still open.
- C10/C11 remain queued behind C6/C7 as before.

## Update: C6 landed this session
The redispatched C6 subagent finished and opened PR #41. Reviewed: exactly the 4 owned files touched,
no chokepoint, BYO-key invariant held (env-var names only), `.dockerignore` correctly excludes `.ai/`.
Image correctly scopes to the gateway sidecar only (documents the daemon as a tenant-layered extension,
since `config.mjs` throws with no injected `DomainPlugin`). `docker build .` itself unverified — this
sandbox blocks `docker --version` the same way it blocked `tasklist`/`curl` earlier — accepted per AC5's
own fallback (subagent verified the exact ENTRYPOINT/CMD path by running `gateway/cli.mjs` directly).
Squash-merged (`gh pr merge 41 --squash --delete-branch`) to `main@45cb3b0`, pulled to primary, re-ran
full suite: 853/862 pass, 1 fail = the known `harness-adapters.test.mjs` flake, not a regression.
Cleaned up the subagent's leftover worktree + local branch. Logged as D-038.

## Next
C6 is done — no longer blocking C11. Remaining queue: C7 (mos-dev, blocked on working-directory access
+ Gate-2 sign-off), C10 (landing page, needs `agy` on PATH check), C11 (commercial docs, now depends
only on C7). Nothing dispatchable this session beyond what's already queued/blocked.

**Schedule confidence:** on track. Two real findings this session (lost C6 dispatch, corrected by
redispatch; webhook POST blocked, logged) plus one card shipped (C6 merged). No founder action needed
except optionally fixing the webhook tool-permission gap.

## Note: Slack webhook digest NOT posted this session
Attempted to POST the resumed digest to the escalation Slack webhook (standing rule: "route every
founder question to the Slack webhook too") and it was blocked by this session's tool-permission
settings — `curl` to an external host requires approval that wasn't granted, unlike the `git`/`gh`/
`npm`/`node`/`npx` allowlist added when the tool-permission gate was lifted (sessions #5–#24 gap,
resolved session #25). Did not keep retrying the identical blocked call. **Founder action, if wanted:**
add a `curl`/webhook-POST allowance to `.claude/settings.local.json` alongside the existing git/gh/npm
grants, the same way the original tool-permission gap was fixed — otherwise every future session's
"post a resumed digest to the webhook" step will silently no-op. This checkpoint file + decision log
serve as the durable record in the meantime.
