# Checkpoint — session #25 (2026-07-18T12:30)

**Resumed digest.** First interactive resume since sessions #16-#19 (founder in-chat). Boot sequence
completed per RESUME-PROMPT.md.

## THE GATE LIFTED
`.claude/settings.local.json` now exists, granting `Bash(git:*)`/`Bash(gh:*)`/`Bash(npm:*)`/
`Bash(node:*)`/`Bash(npx:*)` (deny: `rm -rf`, `git push --force`). This is the tool-permission gap
that blocked test-execution and git-mutation across sessions #5-#24 (21 consecutive sessions, full
narrative in decision-log D-013 through D-033). Verified it's real, not a false positive — `git log`
and `npm test` both ran successfully for the first time.

## What got done this session
- **Diagnosed a comparison trap first:** a naive "run main in a nested worktree" check said main was
  fully green, but that's confounded — `config.mjs`'s `COMPUTED_DEFAULT_ROOT` depends on `config.mjs`'s
  own on-disk location (`join(HERE, '..', '..')`), so a worktree nested under `.claude/worktrees/`
  computes a different (accidentally-valid) root than the primary checkout. Did a true same-path
  comparison instead (stash, checkout main in the primary tree, test, checkout back) to confirm what
  was actually pre-existing vs. introduced by C1/C5.
- **C5 (control-plane MVP) merged** — PR #38 → `main@aa714f0`. Verified `tests/control-plane.test.mjs`
  directly (10/10: AC1/AC1b/AC2/AC2b/AC3/AC4/AC5), full suite in isolation 841/841 pass.
- **C1 (gateway standalone CLI) merged** — PR #39 → `main@b0bb85a`. Verified `gateway/tests/cli.test.mjs`
  directly (8/8: AC1/AC2/AC3/AC3b/AC5).
- **BUG-1 fixed and merged** — PR #40 → `main@3450be1`. Applied the exact diff preserved in
  `.ai/state/BUG-1-fix-pending-commit.md` since session #10 (inject `mkdtempSync` root into
  `tests/bus.test.mjs`'s shared `config`). `npm test` from repo root, no `AIOS_ROOT` set: **854/854
  pass, 0 fail** — first fully-green standalone run since the bug was found. The old
  "no single env greens the whole suite" caveat is retired.
- **Cleanup:** removed the now-obsolete `c5-verify` and `agent-abb3022d0fd4f1036` worktrees/branches,
  deleted merged remote/local feature branches, pruned stale refs.
- **Bookkeeping:** `OWNERSHIP.md` and `continuity.json` updated to reflect all three merges, empty
  `in_flight_cards`/`held_chokepoints`, and the resolved-gate narrative (trimmed the 21-session
  blocker history to a pointer since decision-log D-013–D-033 has the full account). Decision-log
  D-034 appended.

## One flake found, not fixed, not blocking
`tests/harness-adapters.test.mjs` intermittently fails under full-suite concurrent load (different
sub-test each time — `antigravity` mapping, `opencode` harness — always
`TypeError: Cannot read properties of null (reading 'cmd')`). Passes 29/29 deterministically in
isolation, every time tested (3x). Confirmed pre-existing on main *before* any of this session's
merges — not caused by C5/C1/BUG-1 (none of them touch this file). Likely a spawn-concurrency
resource race. Logged as a known issue, low priority, does not block anything.

## Status
- **All in-flight cards from prior sessions are now merged.** `in_flight_cards: []`,
  `held_chokepoints: []` in continuity.json.
- Main is green: `npm test` → 854/854 (the harness-adapters flake is intermittent, not
  deterministic — reran clean once during verification).

## Open founder ask (still unanswered, asked again this session)
Commit the untracked `.ai/` bookkeeping + C8 continuity kit + `OWNERSHIP.md` + the modified
`.gitignore` to `main`? This is orchestrator/process infra, not product code — holding for explicit
sign-off per the sessions #16-#19 precedent, not proceeding unilaterally.

## Next
Once the founder answers the commit question: C6 (Docker/compose L2 packaging), then C7 (mos-dev
thin-tenant rebuild at `/c/projects/mos-dev`). Tooling is fully open now — normal Track-A dispatch
with `isolation: 'worktree'` can resume without any of the session #5-#24 workarounds.

**Schedule confidence:** back on track. The single biggest risk to the frozen 7-day scope (a
21-session tooling stall) is resolved; three cards merged this session alone.
