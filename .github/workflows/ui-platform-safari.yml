name: UI Platform Safari Evidence

on:
  pull_request:
    branches: [main]

jobs:
  safari:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm ci
      - run: sudo safaridriver --enable
      - run: node scripts/safari-ui-platform-evidence.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-platform-safari-evidence
          path: artifacts/browser/safari
