<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 64" fill="none">
  <!-- Icon mark (same as light, tuned for dark bg) -->
  <g transform="translate(0, 0)">
    <!-- Outer ring -->
    <circle cx="32" cy="32" r="29" stroke="#60a5fa" stroke-width="2.5" opacity="0.9"/>
    <!-- Meridian line -->
    <line x1="32" y1="1" x2="32" y2="63" stroke="#60a5fa" stroke-width="2" opacity="0.75"/>
    <!-- Top node -->
    <circle cx="32" cy="8" r="3" fill="#00f0ff"/>
    <!-- Center hub -->
    <circle cx="32" cy="32" r="5.5" fill="#60a5fa"/>
    <circle cx="32" cy="32" r="3" fill="#00f0ff" opacity="0.7"/>
    <!-- Bottom node -->
    <circle cx="32" cy="56" r="3" fill="#00f0ff"/>
    <!-- Orbital arc -->
    <path d="M 14 32 A 18 8 0 0 1 50 32" stroke="#60a5fa" stroke-width="1" opacity="0.4" fill="none"/>
  </g>
  
  <!-- Wordmark (light text for dark bg) -->
  <text x="80" y="42" font-family="'Inter', 'Segoe UI', system-ui, sans-serif" font-weight="600" font-size="26" fill="#f8fafc" letter-spacing="-0.5">
    Meridian<tspan font-weight="300">OS</tspan>
  </text>
</svg>
