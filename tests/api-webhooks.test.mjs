import test from 'node:test'; import assert from 'node:assert/strict'; import { DatabaseSync } from 'node:sqlite'; import { recordWebhookAttempt, listWebhookAttempts } from '../dashboard/management-webhooks.mjs'; import { MANAGEMENT_SCOPE } from './management-fixtures.mjs';
test('management attempt projections never expose the recorded response payload', () => { const db = new DatabaseSync(':memory:'); try { recordWebhookAttempt(db, MANAGEMENT_SCOPE, { webhookId: 'compat-hook', response: 'raw payload' }); assert.equal(JSON.stringify(listWebhookAttempts(db, MANAGEMENT_SCOPE, 'compat-hook')).includes('raw payload'), false); } finally { db.close(); } });
