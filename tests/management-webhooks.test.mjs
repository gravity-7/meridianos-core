import test from 'node:test'; import assert from 'node:assert/strict'; import { recordWebhookAttempt, listWebhookAttempts, replayWebhookAttempt } from '../dashboard/management-webhooks.mjs'; import { MANAGEMENT_SCOPE, FOREIGN_SCOPE } from './management-fixtures.mjs';
test('webhook replay is retained, scoped and idempotent with zero outbound denial', () => { const attempt = recordWebhookAttempt(MANAGEMENT_SCOPE, { webhookId: 'hook-a', status: 'failed', response: 'secret body' }); assert.equal(listWebhookAttempts(MANAGEMENT_SCOPE, 'hook-a').attempts.length > 0, true); const first = replayWebhookAttempt(MANAGEMENT_SCOPE, 'hook-a', attempt.id, 'recover'); const second = replayWebhookAttempt(MANAGEMENT_SCOPE, 'hook-a', attempt.id, 'recover'); const foreign = replayWebhookAttempt(FOREIGN_SCOPE, 'hook-a', attempt.id, 'recover'); assert.equal(first.outbound, true); assert.equal(second.outbound, false); assert.equal(second.outcome, 'duplicate'); assert.equal(foreign.outbound, false); });
